torch:
  device: cuda
  seed: 42
  num_workers: 2

datasets:
  detection:
    dir: "datasets/detection"
    val_split: 0.15
  hold_type_segmentation:
    dir: "datasets/hold_type_segmentation"
    val_split: 0.15

models:
  mask_rcnn_hold:
    pipeline: mask_rcnn
    dataset: "datasets/detection"
    num_classes: 3
    trainable_backbone_layers: 3
    min_size: 1024
    max_size: 1333
    box_detections_per_img: 300
    anchor_sizes: [[16, 32, 64, 128, 256]]
    anchor_ratios: [[0.5, 1.0, 2.0]]
    tile_inference: true
    tile_size: 1024
    tile_overlap: 128
    train:
      batch_size: 4
      lr: 0.005
      epochs: 25
      scheduler: cosine
      lr_milestones: [16, 22]
      lr_gamma: 0.1
    validate:
      batch_size: 1
  mask_rcnn_holdtype:
    pipeline: mask_rcnn
    dataset: "datasets/hold_type_segmentation"
    num_classes: 7
    trainable_backbone_layers: 3
    min_size: 1024
    max_size: 1333
    box_detections_per_img: 300
    anchor_sizes: [[16, 32, 64, 128, 256]]
    anchor_ratios: [[0.5, 1.0, 2.0]]
    tile_inference: false
    tile_size: 1024
    tile_overlap: 128
    train:
      batch_size: 2
      lr: 0.005
      epochs: 30
      scheduler: cosine
      lr_milestones: [20, 26]
      lr_gamma: 0.1
    validate:
      batch_size: 1
